services:
  redis:
    image: redis:alpine
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      start_period: 10s
      interval: 5s
      timeout: 3s
      retries: 3
    volumes:
      - redis:/data
    networks:
      - isolated
